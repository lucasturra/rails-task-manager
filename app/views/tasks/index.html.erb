<h1>Task list</h1>

<ul>
  <% @tasks.each do |task| %>
    <li> <%= link_to task.title, task_path(task) %> </li>
     <% if task.completed %>
        <input type="checkbox">checked>
      <% else %>
        <input type="checkbox">
      <% end %>
  <% end %>
</ul>
<div class="link"><%= link_to 'Create a task', tasks_new_path%></div>
